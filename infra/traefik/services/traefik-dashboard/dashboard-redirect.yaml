apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: dashboard-redirect
  namespace: traefik-system
spec:
  redirectRegex:
    regex: "^https?://traefik.mlops.work/dashboard$"
    replacement: "https://traefik.mlops.work/dashboard/"
    permanent: true
