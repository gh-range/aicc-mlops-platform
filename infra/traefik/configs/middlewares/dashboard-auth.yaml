apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: dashboard-auth
  namespace: traefik-system
  labels:
    app.kubernetes.io/name: traefik
    app.kubernetes.io/component: middleware
spec:
  basicAuth:
    secret: traefik-dashboard-auth
    realm: "Traefik Dashboard - AICC MLOps Platform"
    removeHeader: true
