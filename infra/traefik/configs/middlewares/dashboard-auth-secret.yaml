apiVersion: v1
kind: Secret
metadata:
  name: traefik-dashboard-auth
  namespace: traefik-system
  labels:
    app.kubernetes.io/name: traefik
    app.kubernetes.io/component: dashboard-auth
type: Opaque
data:
  users: $(cat /tmp/traefik-auth | base64 -w 0)
